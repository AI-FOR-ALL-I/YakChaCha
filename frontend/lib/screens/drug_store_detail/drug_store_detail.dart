import 'package:flutter/material.dart';

class DrugStoreDetail extends StatelessWidget {
  const DrugStoreDetail({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}