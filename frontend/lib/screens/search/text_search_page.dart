import 'package:flutter/material.dart';

class TextSearchPage extends StatelessWidget {
  const TextSearchPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('durl'));
  }
}
