import 'package:flutter/material.dart';

class DrugHistoryPage extends StatelessWidget {
  const DrugHistoryPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('This is a Fourth Page'),
    );
  }
}
